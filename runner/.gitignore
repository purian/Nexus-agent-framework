node_modules
dist
*.log
.DS_Store

# Local secrets — should never live in this folder anyway, but belt + braces.
.env
.env.*
secrets.env
secrets.json

# launchd plist gets generated by install-launchd.sh
*.plist
